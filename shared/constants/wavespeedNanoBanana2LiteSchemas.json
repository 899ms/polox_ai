{
  "google/nano-banana-2-lite/text-to-image": {
    "additionalProperties": false,
    "properties": {
      "aspect_ratio": {
        "default": "1:1",
        "description": "Output aspect ratio.",
        "enum": [
          "1:1",
          "3:2",
          "2:3",
          "3:4",
          "4:3",
          "4:5",
          "5:4",
          "9:16",
          "16:9",
          "21:9",
          "1:4",
          "4:1",
          "1:8",
          "8:1"
        ],
        "title": "Aspect Ratio",
        "type": "string",
        "x-placeholder": "Select Aspect Ratio",
        "x-ui-component": "select"
      },
      "enable_base64_output": {
        "default": false,
        "description": "If set to `true`, the prediction's `output` strings are returned as **naked base64** (no `data:<mime>;base64,` prefix). When `false` (default), outputs are returned as URLs pointing to our CDN.",
        "disabled": true,
        "type": "boolean"
      },
      "enable_sync_mode": {
        "default": false,
        "description": "If set to `true`, the request attempts to wait for the generated result and return outputs in the same response. If the result is not ready within the sync wait window, the API can return a timeout body while the task continues processing. This option is only available via the API and is supported only by some models.",
        "disabled": true,
        "type": "boolean"
      },
      "output_format": {
        "default": "png",
        "description": "Format of the generated image.",
        "enum": [
          "png",
          "jpeg"
        ],
        "title": "Output Format",
        "type": "string",
        "x-ui-component": "select"
      },
      "prompt": {
        "description": "Text prompt describing the image to generate.",
        "title": "Prompt",
        "type": "string",
        "x-rows": 4,
        "x-ui-component": "textarea"
      }
    },
    "required": [
      "prompt"
    ],
    "type": "object",
    "x-order-properties": [
      "prompt",
      "aspect_ratio",
      "output_format",
      "enable_sync_mode",
      "enable_base64_output"
    ]
  },
  "google/nano-banana-2-lite/edit": {
    "additionalProperties": false,
    "properties": {
      "aspect_ratio": {
        "description": "The aspect ratio of the generated image. Auto-detected from input image if not specified.",
        "enum": [
          "1:1",
          "3:2",
          "2:3",
          "3:4",
          "4:3",
          "4:5",
          "5:4",
          "9:16",
          "16:9",
          "21:9",
          "1:4",
          "4:1",
          "1:8",
          "8:1"
        ],
        "type": "string",
        "x-placeholder": "Select aspect ratio"
      },
      "enable_base64_output": {
        "default": false,
        "description": "If set to `true`, the prediction's `output` strings are returned as **naked base64** (no `data:<mime>;base64,` prefix). When `false` (default), outputs are returned as URLs pointing to our CDN.",
        "disabled": true,
        "type": "boolean"
      },
      "enable_sync_mode": {
        "default": false,
        "description": "If set to `true`, the request attempts to wait for the generated result and return outputs in the same response. If the result is not ready within the sync wait window, the API can return a timeout body while the task continues processing. This option is only available via the API and is supported only by some models.",
        "disabled": true,
        "type": "boolean"
      },
      "images": {
        "description": "Input image URLs for editing.",
        "items": {
          "type": "string"
        },
        "maxItems": 4,
        "title": "Images",
        "type": "array"
      },
      "output_format": {
        "default": "png",
        "description": "Format of the edited image.",
        "enum": [
          "png",
          "jpeg"
        ],
        "title": "Output Format",
        "type": "string",
        "x-ui-component": "select"
      },
      "prompt": {
        "description": "Edit instruction describing how to transform the input image.",
        "title": "Prompt",
        "type": "string",
        "x-rows": 4,
        "x-ui-component": "textarea"
      }
    },
    "required": [
      "images",
      "prompt"
    ],
    "type": "object",
    "x-order-properties": [
      "images",
      "prompt",
      "aspect_ratio",
      "output_format",
      "enable_sync_mode",
      "enable_base64_output"
    ]
  }
}
