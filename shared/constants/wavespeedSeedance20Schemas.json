{
  "bytedance/seedance-2.0/text-to-video": {
    "additionalProperties": false,
    "properties": {
      "aspect_ratio": {
        "default": "16:9",
        "description": "The aspect ratio of the generated video.",
        "enum": [
          "16:9",
          "9:16",
          "4:3",
          "3:4",
          "1:1",
          "21:9"
        ],
        "type": "string",
        "x-ui-component": "select"
      },
      "duration": {
        "default": 5,
        "description": "The duration of the generated video in seconds (4-15s).",
        "maximum": 15,
        "minimum": 4,
        "type": "integer",
        "x-ui-component": "slider"
      },
      "enable_web_search": {
        "default": false,
        "description": "Enable web search for real-time information.",
        "type": "boolean"
      },
      "generate_audio": {
        "default": true,
        "description": "Whether to generate native audio synchronized with the output video. Defaults to true.",
        "type": "boolean"
      },
      "prompt": {
        "description": "Describe the scene, action, camera movement, and mood for the video.",
        "type": "string"
      },
      "reference_audios": {
        "description": "Reference audio URLs (total length must not exceed 15 seconds).",
        "items": {
          "type": "string"
        },
        "maxItems": 3,
        "type": "array"
      },
      "reference_images": {
        "description": "Reference image URLs to guide visual style, characters, or scene composition.",
        "items": {
          "type": "string"
        },
        "maxItems": 9,
        "type": "array"
      },
      "reference_videos": {
        "description": "Reference video URLs (total length must not exceed 15 seconds).",
        "items": {
          "type": "string"
        },
        "maxItems": 3,
        "type": "array"
      },
      "resolution": {
        "default": "720p",
        "description": "The output video resolution.",
        "enum": [
          "480p",
          "720p",
          "1080p",
          "4k"
        ],
        "type": "string",
        "x-ui-component": "select"
      }
    },
    "required": [
      "prompt"
    ],
    "type": "object",
    "x-order-properties": [
      "prompt",
      "reference_images",
      "reference_videos",
      "reference_audios",
      "aspect_ratio",
      "resolution",
      "duration",
      "enable_web_search",
      "generate_audio"
    ]
  },
  "bytedance/seedance-2.0/image-to-video": {
    "additionalProperties": false,
    "properties": {
      "aspect_ratio": {
        "description": "The aspect ratio of the generated video. If not specified, adapts to the input image.",
        "enum": [
          "16:9",
          "9:16",
          "4:3",
          "3:4",
          "1:1",
          "21:9"
        ],
        "type": "string",
        "x-ui-component": "select"
      },
      "duration": {
        "default": 5,
        "description": "The duration of the generated video in seconds (4-15s).",
        "maximum": 15,
        "minimum": 4,
        "type": "integer",
        "x-ui-component": "slider"
      },
      "enable_web_search": {
        "default": false,
        "description": "Enable web search for real-time information.",
        "type": "boolean"
      },
      "generate_audio": {
        "default": true,
        "description": "Whether to generate native audio synchronized with the output video. Defaults to true.",
        "type": "boolean"
      },
      "image": {
        "description": "Start image URL to guide the video generation.",
        "type": "string"
      },
      "last_image": {
        "description": "Last frame image URL for video continuation.",
        "type": "string"
      },
      "prompt": {
        "description": "Describe the scene, action, camera movement, and mood for the video.",
        "type": "string"
      },
      "resolution": {
        "default": "720p",
        "description": "The output video resolution.",
        "enum": [
          "480p",
          "720p",
          "1080p",
          "4k"
        ],
        "type": "string",
        "x-ui-component": "select"
      }
    },
    "required": [
      "prompt",
      "image"
    ],
    "type": "object",
    "x-order-properties": [
      "prompt",
      "image",
      "last_image",
      "aspect_ratio",
      "resolution",
      "duration",
      "enable_web_search",
      "generate_audio"
    ]
  },
  "bytedance/seedance-2.0/video-edit": {
    "additionalProperties": false,
    "properties": {
      "aspect_ratio": {
        "description": "Aspect ratio of the output video. Adapts to the input if not specified.",
        "enum": [
          "16:9",
          "9:16",
          "4:3",
          "3:4",
          "1:1",
          "21:9"
        ],
        "type": "string",
        "x-ui-component": "select"
      },
      "duration": {
        "description": "Output video length in seconds (4-15). Auto-detected from the input video if not specified.",
        "maximum": 15,
        "minimum": 4,
        "type": "integer",
        "x-hidden": true,
        "x-ui-component": "slider"
      },
      "enable_web_search": {
        "default": false,
        "description": "Enable web search for real-time information.",
        "type": "boolean"
      },
      "generate_audio": {
        "default": true,
        "description": "Whether to generate native audio for the edited output. Defaults to true. When set to false, the input video's audio track is preserved on the output instead.",
        "type": "boolean"
      },
      "prompt": {
        "description": "Describe the edit you want applied to the input video.",
        "type": "string"
      },
      "reference_audios": {
        "description": "Optional reference audio URLs to guide audio generation.",
        "items": {
          "type": "string"
        },
        "maxItems": 3,
        "type": "array"
      },
      "reference_images": {
        "description": "Optional reference image URLs to guide the edit (subject identity, style, etc.).",
        "items": {
          "type": "string"
        },
        "maxItems": 9,
        "type": "array"
      },
      "resolution": {
        "default": "720p",
        "description": "Output video resolution.",
        "enum": [
          "480p",
          "720p",
          "1080p",
          "4k"
        ],
        "type": "string",
        "x-ui-component": "select"
      },
      "video": {
        "description": "URL of the input video to edit. Videos longer than 15s are trimmed to 15s.",
        "type": "string"
      }
    },
    "required": [
      "prompt",
      "video"
    ],
    "type": "object",
    "x-order-properties": [
      "prompt",
      "video",
      "reference_images",
      "reference_audios",
      "aspect_ratio",
      "resolution",
      "duration",
      "enable_web_search",
      "generate_audio"
    ]
  }
}
