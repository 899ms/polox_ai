{
  "bytedance/seedream-v5.0-pro": {
    "additionalProperties": false,
    "properties": {
      "aspect_ratio": {
        "default": "1:1",
        "description": "The aspect ratio of the generated image.",
        "enum": [
          "1:1",
          "1:2",
          "2:1",
          "1:3",
          "3:1",
          "2:3",
          "3:2",
          "3:4",
          "4:3",
          "4:5",
          "5:4",
          "9:16",
          "16:9",
          "9:21",
          "21:9"
        ],
        "type": "string",
        "x-placeholder": "Select aspect ratio"
      },
      "enable_base64_output": {
        "default": false,
        "description": "If set to `true`, the prediction's `output` strings are returned as **naked base64** (no `data:<mime>;base64,` prefix). When `false` (default), outputs are returned as URLs pointing to our CDN.",
        "disabled": true,
        "type": "boolean"
      },
      "enable_sync_mode": {
        "default": false,
        "description": "If set to `true`, the request attempts to wait for the generated result and return outputs in the same response. If the result is not ready within the sync wait window, the API can return a timeout body while the task continues processing. This option is only available via the API and is supported only by some models.",
        "disabled": true,
        "type": "boolean"
      },
      "output_format": {
        "default": "jpeg",
        "description": "The format of the output image.",
        "enum": [
          "jpeg",
          "png"
        ],
        "type": "string"
      },
      "prompt": {
        "description": "The positive prompt for the generation.",
        "type": "string",
        "x-rows": 10,
        "x-ui-component": "textarea"
      },
      "prompt_optimization_mode": {
        "default": "standard",
        "description": "Prompt optimization mode. The model rewrites your prompt before generating. 'standard' uses the full rewrite; 'fast' uses a lighter one that generates several times quicker but follows long or intricate prompts less closely, and is served only by the upstream providers that honour it, so it has less failover depth.",
        "enum": [
          "standard",
          "fast"
        ],
        "type": "string",
        "x-ui-component": "select"
      },
      "resolution": {
        "default": "1k",
        "description": "The output resolution tier used for billing. 1k is the lower-cost tier; 2k is the higher-cost tier.",
        "enum": [
          "1k",
          "1.5k",
          "2k"
        ],
        "type": "string"
      }
    },
    "required": [
      "prompt"
    ],
    "type": "object",
    "x-order-properties": [
      "prompt",
      "aspect_ratio",
      "resolution",
      "output_format",
      "prompt_optimization_mode",
      "enable_sync_mode",
      "enable_base64_output"
    ]
  },
  "bytedance/seedream-v5.0-pro/edit": {
    "additionalProperties": false,
    "properties": {
      "aspect_ratio": {
        "description": "The aspect ratio of the generated image. Leave empty to automatically use the closest supported aspect ratio based on the first input image.",
        "enum": [
          "1:1",
          "1:2",
          "2:1",
          "1:3",
          "3:1",
          "2:3",
          "3:2",
          "3:4",
          "4:3",
          "4:5",
          "5:4",
          "9:16",
          "16:9",
          "9:21",
          "21:9"
        ],
        "type": "string",
        "x-placeholder": "Select aspect ratio"
      },
      "enable_base64_output": {
        "default": false,
        "description": "If set to `true`, the prediction's `output` strings are returned as **naked base64** (no `data:<mime>;base64,` prefix). When `false` (default), outputs are returned as URLs pointing to our CDN.",
        "disabled": true,
        "type": "boolean"
      },
      "enable_sync_mode": {
        "default": false,
        "description": "If set to `true`, the request attempts to wait for the generated result and return outputs in the same response. If the result is not ready within the sync wait window, the API can return a timeout body while the task continues processing. This option is only available via the API and is supported only by some models.",
        "disabled": true,
        "type": "boolean"
      },
      "images": {
        "description": "The images to edit. A maximum of 10 reference images can be uploaded.",
        "items": {
          "type": "string"
        },
        "maxItems": 10,
        "type": "array"
      },
      "output_format": {
        "default": "jpeg",
        "description": "The format of the output image.",
        "enum": [
          "jpeg",
          "png"
        ],
        "type": "string"
      },
      "prompt": {
        "description": "The positive prompt for the generation.",
        "type": "string"
      },
      "prompt_optimization_mode": {
        "default": "standard",
        "description": "Prompt optimization mode. The model rewrites your prompt before generating. 'standard' uses the full rewrite; 'fast' uses a lighter one that generates several times quicker but follows long or intricate prompts less closely, and is served only by the upstream providers that honour it, so it has less failover depth.",
        "enum": [
          "standard",
          "fast"
        ],
        "type": "string",
        "x-ui-component": "select"
      },
      "resolution": {
        "default": "1k",
        "description": "The output resolution tier used for billing. 1k is the lower-cost tier; 2k is the higher-cost tier.",
        "enum": [
          "1k",
          "1.5k",
          "2k"
        ],
        "type": "string"
      }
    },
    "required": [
      "prompt",
      "images"
    ],
    "type": "object",
    "x-order-properties": [
      "prompt",
      "images",
      "aspect_ratio",
      "resolution",
      "output_format",
      "prompt_optimization_mode",
      "enable_sync_mode",
      "enable_base64_output"
    ]
  }
}
